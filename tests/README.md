# Tests

Automated tests will be introduced as the implementation is extracted from the historical notebook and the experimental pipeline is repaired.

This directory is created now to establish the repository layout; its presence does not imply that automated validation already exists.
